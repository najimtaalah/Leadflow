# PostgreSQL
POSTGRES_PASSWORD=changeme_strong_password

# JWT — générer avec: openssl rand -hex 32
JWT_SECRET=changeme_at_least_32_chars_hex_secret
JWT_REFRESH_SECRET=changeme_refresh_at_least_32_chars_secret
